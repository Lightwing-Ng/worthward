Flask==3.1.3
pandas==2.3.3
pyarrow==23.0.1
yfinance==1.5.1
# urllib3 2.x requires OpenSSL 1.1.1 or later. The supported runtime uses OpenSSL 3.
urllib3==2.7.0
longbridge==4.0.5
openpyxl==3.1.5
curl_cffi~=0.15.0
certifi>=2024.8.30
numpy~=2.3.5
# Bayesian and LSTM Apple Silicon/CUDA acceleration. The application still
# falls back to the deterministic NumPy CPU backend when no device is usable.
torch==2.14.0

# Development and quality dependencies pinned for the supported runtime and quality gate.
pytest==9.0.3
pytest-cov==7.1.0
ruff==0.15.21
playwright==1.61.0
