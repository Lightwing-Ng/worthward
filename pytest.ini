[pytest]
testpaths = tests
python_files = test_*.py
addopts = --strict-markers
markers =
    integration: Flask integration tests that cross module boundaries.
    slow: Tests that take materially longer than the unit-test median.
